[project]
name = "Flask_Sorting"
version = "0.1.0"
description = "My small project"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["Game", "Tests"]